Al enviar una petición POST a la ruta /registro con un objeto JSON que contiene el nombre y el mensaje, el servidor responde con un objeto JSON confirmando que los datos fueron recibidos correctamente y devuelve la información enviada.
